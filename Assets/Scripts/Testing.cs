using UnityEngine;

public class Testing : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Grid testing = new Grid(4, 4);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
